<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sp.senac.card">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Fotos"></activity>
        <activity android:name=".Noticias" />
        <activity android:name=".Cards" />
        <activity android:name=".Tempo" />
        <activity android:name=".Frutas" />
        <activity android:name=".Moeda" />
        <activity android:name=".musica" />
        <activity android:name=".MainActivity" android:theme="@style/MeuTema">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>